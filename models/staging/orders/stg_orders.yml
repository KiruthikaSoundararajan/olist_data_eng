version: 2

models:
  - name: stg_orders
    columns:
    - name: order_status
      tests:
        - not_null
        - accepted_values:
            values: [ 'shipped' ,'unavailable','invoiced','created','approved','processing','delivered','canceled']